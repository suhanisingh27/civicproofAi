# No environment variables are required for the local VS Code version.
# CivicProof uses local account storage, local complaint storage, and browser AI.
# Optional: the public production URL, used for canonical and Open Graph links.
NEXT_PUBLIC_SITE_URL=https://civicproof.example.com
